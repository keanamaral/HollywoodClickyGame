import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>HOLLYWOOD</h1>
        <h2>Clicky Game! Scroll down to play...</h2>
        <p>Click on an image below to earn points,</p>
        <p>But ONLY once per image!</p>
    </div>
);

export default Title;